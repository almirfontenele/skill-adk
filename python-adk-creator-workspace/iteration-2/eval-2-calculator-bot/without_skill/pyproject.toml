[project]
name = "calculator-bot"
version = "0.1.0"
description = "A calculator bot using Google ADK 2.0 and Gemini"
requires-python = ">=3.11"
dependencies = [
    "google-adk>=1.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
